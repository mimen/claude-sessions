---
description: Move the current Saved session back to the active CCS working set
allowed-tools: Bash(ccs:*)
---

# Move this session to Active

Run the following command, then report the resulting lifecycle:

```sh
ccs session unsave .
```
