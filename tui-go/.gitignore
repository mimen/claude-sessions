# Build output. The binary is built on demand by src/tui-go/launch.ts when a Go
# source file is newer than it, so it never belongs in the repo.
.bin/

# Git worktrees used by delegated sessions.
.worktrees/
