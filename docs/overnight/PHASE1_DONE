2026-07-16T02:00Z phase-1 complete

## Summary

Overnight hardening (Phase 1) is complete. All 13 acceptance criteria verified + locked in with regression tests. Punch list exhausted and stayed empty for 2 consecutive ticks (01:00Z, 01:30Z).

## Artifacts

- `docs/overnight/phase1-hardening.md` — all 13 acceptance boxes checked with commit hashes
- `docs/overnight/log.md` — 40 ticks, 22 fixes + 18 ruled-out findings, all with regression locks
- `docs/overnight/surface-coverage.md` — 38 subcommands verified fresh-CCS_ROOT safe
- Test suite: 734 tests, 0 failures, bun typecheck clean

## Notable fixes (high-severity)

1. **mintIdentity TOCTOU race** (667475b) — concurrent mint of same key could create 2 rows; now atomic with INSERT...ON CONFLICT.
2. **Empty identity_key validation** (1a29d06) — mint accepted empty/whitespace/control-char keys; now guarded by `assertIdentityKeyOk`.
3. **Fresh CCS_ROOT crashes** (multiple commits) — 5 sites where openCatalogue was called without ensureDataDir() on fresh `.ccs` dir; now systematic + regression-locked.
4. **dedup-sessions-per-identity non-idempotent** (d06c4c0) — setArchived bumped updated_at, flipped keeper selection on each run; fixed with active-only filter.

## Next phase

Phase 2 (`overnight-dogfood`) will exercise the tool end-to-end under realistic workloads to prove stability. See spawned session for details.
