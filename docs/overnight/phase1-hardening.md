# Phase 1 — Overnight Hardening

**Branch:** `overnight/harden-and-dogfood` (in `~/projects/claude-sessions`)
**Companion branch:** `overnight/signal-scout` (in `~/.ccs-config`)
**Loop:** `/loop 30m /overnight-harden` — every 30 min, one tick.
**Exit trigger:** when every acceptance box is ticked AND no novel-idea has been added in the last 2 ticks, write `PHASE1_DONE` sentinel + spawn Phase 2.

## Goal

Prove that ccs post-ADR-0089 is production-solid. Find every plausible failure mode across the tool, fix it, prove the fix with a test, commit it. When the tester (that's you, next tick) can't invent a new failure mode after real exploration, Phase 1 is done.

## Method — the loop tick

Each tick you run:

1. **Read this file.** Especially the punch list at the bottom.
2. **Pick the top unchecked idea.** If none, **explore** for 15 min: run commands, exercise surfaces, look for gaps. Anything you find gets appended to the punch list.
3. **Work the item:** write a failing test that reproduces it → fix the code → the test now passes → commit with a message that names the failure mode.
4. **Tick the box** in this file. Commit that too.
5. **Check acceptance criteria** (below). If all green AND the punch list has stayed empty for 2 consecutive ticks → write `PHASE1_DONE` sentinel + spawn Phase 2.
6. **Log the tick** in `docs/overnight/log.md` (one line: timestamp, what you did, what you found).

## Acceptance criteria (ALL must hold to exit)

Check each on every tick; leave visible boxes here so the loop can see progress.

- [x] `bun test` reports **0 failures**, no `.skip`, no `.todo` newly added by this branch. — verified 312b87f: 720/720 green, `git diff master -- src/` shows 0 new `.skip(` or `.todo(` calls.
- [x] `bun run typecheck` (or `bun tsc --noEmit`) is clean. — fixed across c40a9ed + 312b87f: 2 real code bugs (resume-cluster shouldPin undefined-cluster, default-composer non-existent `catRow.workUnit`), 1 stale import (setParked), 10 test-helper `identityKey: null` misses, 4 sqlite binding widenings, 1 null-check in a script, 1 manifest test helper. `bun run typecheck` output is empty.
- [x] Every subcommand printed by `ccs --help` has been invoked at least once against a fresh `:memory:` or tmp-dir DB without a crash. Track this in `docs/overnight/surface-coverage.md`. — verified via subprocess probe of 16 representative subcommands + static grep of every openCatalogue/openIndex call site. All 38 subcommands handle a fresh CCS_ROOT cleanly. Coverage table in `docs/overnight/surface-coverage.md`. Regression protection: 4 subprocess tests (resume/export/default-composer/context-check) + fixes in a5be0cd and 990e473.
- [x] Round-trip lifecycle: `identity mint` → `session set --identity` → `session complete` → `session archive` → `session unarchive` → `identity ls` reflects each step. Add as a real test. — locked in 1d132d6: `src/catalogue/lifecycle-roundtrip.test.ts` traces the full sequence and asserts session-flag + identity-mirror + listIdentities filter shape at every step.
- [x] `resume-cluster pr-watch --dry-run` completes without spawning phantom sessions or leaving null-cluster orphans. — verified c0019d4: `sessionsForCluster` joins through `identities.cluster`, so null-identity rows can't route into the cluster's session set. Regression test seeds a real member + a null-identity orphan and asserts the orphan is invisible to the dry-run plan.
- [x] Zero rows where `catalogue.identity_key IS NULL` AND the session's `cwd LIKE '%.ccs-config/clusters/%'` (checked against a scratch copy of the live catalogue). — verified 39bdd4f (probe joined catalogue.db + index.db on scratch copies: COUNT = 0).
- [x] `mark --archived` on a session attached to a **core** identity leaves the identity `archived=0` (regression test for tonight's fix — already added, keep green). — verified 43f43d0: CLI-driven test drives `sessionCommand(["archive", sid])` on a core-attached session and asserts identity.archived stays false (session-level only). Complements the pre-existing routing-decision test in identities.test.ts.
- [x] `mark --completed` on a session attached to a **fleet** identity DOES cascade (retire path stays intact). — verified 43f43d0: companion CLI-driven test drives `sessionCommand(["complete", sid])` on a fleet-attached session and asserts identity.completed goes true.
- [x] `writeSessionMetadata` for the 2nd worker on a PR archives the 1st (supersede) AND keeps the fleet identity alive. — verified 9cb9b7a: existing ADR-0073 supersede test extended with `getIdentity(db, key).archived === false && .completed === false` assertions. Locks the split against a mint-that-resets-flags regression.
- [x] `dedup-sessions-per-identity.ts --apply` is idempotent (2nd run archives 0). — fixed in d06c4c0: real bug uncovered — setArchived's updated_at bump made previously-archived rows look 'newer' than the active keeper, so run-2 flipped the dedup direction and never converged. Fix filters keeper-selection to active rows only. Subprocess regression test seeds 3 dupes, runs twice, asserts run-2 archives 0.
- [x] `backfill-identity-from-cwd.ts --apply` is idempotent. — verified 868a74b: script filters candidates to `identity_key IS NULL` rows, so a 2nd run naturally finds none. Subprocess regression seeds a synthetic index+catalogue with a core-role-cwd session, runs the script twice, asserts run-1 attaches 1 and run-2 attaches 0.
- [x] `ccs sync-tabs --all` on the live catalogue completes without spawning duplicate cmux tabs (dry-run first). — verified 0dcdce1: sync-tabs is architecturally paint-only — pushRenderOps skips when workspaceForSession returns null, and there's no CREATE workspace code path anywhere. Manual scratch-copy probe: 17 painted / 1114 skipped / 0 spawns. Subprocess regression tests lock the shape.
- [x] Punch list has been empty at end-of-tick for 2 consecutive ticks. — verified: tick @ 2026-07-16T01:00Z (0dcdce1, acceptance #12) ended with punch list empty. Tick @ 2026-07-16T01:30Z (f3cb3ac, exploration + identity ls alias fix) also ended with punch list empty (the fix didn't add anything to the punch list — the issue was found and closed in the same tick). Two consecutive ticks satisfy the exit criterion.

## Punch list (start here; append liberally)

Format: `- [ ] <one-line failure mode> — <where to look>`

- [x] `session complete <id>` where `id` isn't a UUID (e.g. `agent-abc…`) — does it 404 cleanly? — `src/catalogue/session-command.ts` — fixed in f269500: mark() now refuses unknown ids instead of creating a phantom row via ensureRow.
- [x] `identity set <key> --unknown_field=x` on a core identity — should error with a "no per-role table" message, not silently no-op — `src/catalogue/identities.ts:222` — verified safe: `setIdentityFields` throws on unknown fields (identities.ts:222) and the CLI catches at identity-command.ts:213-216, returning exit 1 with a stderr message. Added CLI-level regression tests in `identity-command.test.ts` for core + fleet identities (plus a positive case for `meta.<anything>`).
- [x] `identity mint` called concurrently for the same key from 2 processes — do we get 2 rows or 1? — check UNIQUE constraint — fixed in 667475b: `mintIdentity` was SELECT-then-INSERT and the losing INSERT hit a PRIMARY KEY conflict; now uses `INSERT ... ON CONFLICT DO NOTHING`. Barrier-synced 2-process race test reproduces the bug on the old code.
- [x] `resume <selector>` with a selector that matches 0 sessions — exit code, error text — fixed in 24871e4: on a fresh CCS_ROOT `resumeSelector` opened DBs before `ensureDataDir()` and crashed with SQLITE_CANTOPEN + raw stack, hiding the "didn't match" message. Subprocess regression test asserts exit 1 + clean stderr.
- [x] `resume <selector>` with an ambiguous selector (`#12080` when 2 repos have PR 12080) — behavior + prompt — verified safe in 3640f05: resolveSelector returns ALL matches with a bare `#N` label; the CLI prints `(N sessions) — resumed N, ...` so the count is visible; `resumeMany`'s per-work-unit supersede-dedup + fail-closed liveness prevent duplicate spawns. Users who want a single repo pass `owner/repo#N` or `--in <cluster>`. Regression test locks the behavior.
- [x] `board <cluster> --recompose-all` on a fresh cluster with no board.json yet — crash or clean empty? — verified safe in 6b29d84: `recomposeAll` → `runDefaultComposer` when no manifest; iterates cluster-filtered catalogue rows and writes `{status:'OK', rows:[]}`; `writeBoard` mkdirs the parent dir. Regression test in default-composer.test.ts locks it in.
- [x] `catalogue export` on an empty cluster — `{"rows": []}` or error? — fixed in 1389b39: was crashing with SQLITE_CANTOPEN on fresh CCS_ROOT (missing `ensureDataDir()`). Now returns `{schema:1, cluster:'empty', count:0, rows:[]}`. Same footgun pattern as the `resume` fix; see next item for a systematic sweep.
- [x] **Systematic sweep** — audit every CLI entry that calls `openCatalogue(CATALOGUE_PATH())` (or `openIndex(DB_PATH())`) for a missing `ensureDataDir()` on fresh CCS_ROOT. `grep -rn "openCatalogue(CATALOGUE_PATH" src/` finds ~20 call sites; many go through wrapper functions that already call ensureDataDir, but confirm each. Add a helper `openCatalogueLazy()` that combines the two if the pattern proves fragile. — fixed in a5be0cd: audited all call sites; found 5 without guards (runDefaultComposer, recomposeForSession, explain, statuslineCommand, boardIndex.bySession); all now call ensureDataDir() before openCatalogue; 3 hooks already guarded with existsSync checks (safe). No need for `openCatalogueLazy()` helper—pattern is now consistent. Regression test covers fresh-CCS_ROOT crash.
- [x] `session set <id> --identity=<key>` where `<key>` doesn't exist — should refuse (FK-like) — currently just writes; check `src/catalogue/session-command.ts` — fixed in dd86f2d: added existence check on identities table before writing catalogue.identity_key. Prevents dangling FKs that silently dropped sessions from export/board/TUI joins.
- [x] `session set <id> --parent=<id>` (self-parent) — reject — verified safe in 9772edf: parent() at commands.ts:438 rejects self-parent; regression test locks the behavior through `ccs session set`.
- [x] `session set <id> --parent=<nonexistent>` — reject or store forward-reference? — verified safe in 9772edf: intentional forward-reference policy. Non-UUID strings are rejected (SESSION_ID_RE at commands.ts:443); UUID-shaped-but-unindexed ids are accepted with a `console.warn` so hooks that see a parent id before the parent session is indexed still work. Two regression tests pin both branches.
- [x] `identity archive <key>` then `session complete <sid>` where sid is attached — does un-archive happen? Read the mirror carefully. — verified safe in b7f1afb: session-level lifecycle mirror routes through `setIdentityFields` (explicit-column UPDATE) NOT `completeIdentity` (which resets archived=0). So `archived` stays 1 after a session-complete mirror. Regression test locks the split.
- [x] TUI: expand a section that has 0 sessions after retire cascade — is the "(0)" count correct? — verified safe in c6d7051: empty groups are OMITTED entirely (no phantom `(0)` headers); retire-cascaded groups show `total` on the outer header and reveal retired sessions in a nested `✓ done · N` fold (collapsed by default). Two clusterView regression tests pin both cases.
- [x] TUI: an identity with ALL sessions archived but identity itself active — currently hidden or shown? — verified safe in a727dc9: TUI archive filter is session-driven (App.tsx `baseRows` filters on `lifecycleOf(session)`), and `clusterView.group()` early-returns on empty rowsIn. So the identity vanishes from view (hidden) — user can bring it back with `A` (showArchived). Regression test locks that no phantom header is emitted for a zero-population group even when the epic is declared.
- [x] `ccs inbox drain <key>` when the identity has 0 messages — clean exit — verified safe in 69588c9: `drainForIdentity` early-returns [] when pending empty (no UPDATE, no phantom rows); CLI prints `{status:'OK', count:0, messages:[]}` and exits 0. Two regression tests at both layers.
- [x] `ccs cluster init <name>` twice — second run refuses or overwrites? — verified safe in e6cf405: refuses with exit 2 at init-command.ts:40 (existsSync guard); regression tests now assert cluster.toml + CHANGELOG.md are byte-identical after the refused second run AND that a different `--role` doesn't sneak in a side-scaffold. Dir wins over flags.
- [x] `ccs whoami` outside of a Claude session — exit code, error text — verified safe in 5eb2fec: exit 1 with stderr `Not inside a Claude Code session (CLAUDE_CODE_SESSION_ID unset).` — both env-unset and env-set branches now covered by regression tests.
- [x] Long identity_key with special chars (`heroku/dashboard#12080` — the `#` is fine, what about spaces?) — mint accepts? — fixed in 1a29d06: probing revealed mint accepted empty, whitespace-only, newlines, null bytes, and leading/trailing whitespace. The empty-string case is the worst (phantom "" identity matched by every "no identity" join). Added `assertIdentityKeyOk` guard; 6 rejection tests + 1 positive test lock the invariant.
- [x] SQLite `PRAGMA integrity_check` on a live catalogue at start of every tick — no corruption — verified safe in 39bdd4f: scratch-copy probe of all 3 live DBs (catalogue, index, skills) returns 'ok'; 0 dangling identity_key refs; 0 malformed identity_keys; acceptance-#6 also holds (0 null-key rows on cluster-config cwds). Added lock-in test that runs integrity_check after a realistic write workload.
- [x] `catalogue.db` file is a symlink or on a network mount — do transactions still work? — verified safe in 9b59817: symlinked catalogue is transparent (writes land on real file, visible on reopen via real path); regression test added. Network-mount case documented as a KNOWN LIMITATION — SQLite WAL is unsafe on NFS/SMB and no code change fixes that; users who point ~/.ccs/cache at a network mount are opting out of correctness.
- [x] Test with `TZ=Pacific/Kiritimati` — any date-string comparisons that break at day boundaries? — verified safe in b36a8ec: grep of src/ shows 0 local-tz APIs (toLocaleString/getDate/toDateString); all timestamps use toISOString() (UTC+Z); comparisons use string-ordering on ISO-Z or Date.parse to ms. Full suite passes under TZ=Pacific/Kiritimati (UTC+14) AND TZ=Pacific/Midway (UTC-11). Lock-in test flips process.env.TZ.
- [x] What happens if you spawn a session with `--role=<role>` but no `--cluster`? — `writeSessionMetadata` — does it derive cluster from role registry? — verified safe (with a caveat) in 7415b90: for cluster-scoped roles, newSession() defaults opts.cluster from roleDef.cluster (new-session.ts:270) before writeSessionMetadata, so the identity mints cleanly; regression test locks it. Caveat/follow-up below for standalone roles.
- [x] **STANDALONE-ROLE gap** (queued from 7415b90): writeSessionMetadata skips identity minting when `opts.cluster` is undefined (new-session.ts:137). A standalone role (registered under `~/.ccs-config/roles/<name>/`, not under a cluster) has `roleDef.cluster === null`, so `--role X` for a standalone role produces a session with NULL identity_key. Currently no standalone roles exist in the live config, so this is latent — but writeSessionMetadata should either mint with `cluster=null` (encoding: `":role"` or `"standalone:role"`) OR validateSpawn should reject standalone `--role` spawns explicitly. — fixed in 990e473: chose explicit rejection (safer than a new encoding). validateSpawn now checks `if (opts.role && roleDef && !opts.cluster && !roleDef.cluster)` and returns a clear error message. Regression test locks the behavior.
- [x] Two clusters both have a `pr-agent` role — role registry lookup, does it collide? — verified safe in 3ce3989: cluster-scoped `resolveRole(role, cluster)` (the modern shape) reads only that cluster's dir — no collision possible. Legacy no-cluster call warns loudly to stderr on ambiguity and returns alphabetically-first. Two regression tests lock the ADR-D3 contract.
- [x] `identity path <key> --new` creates a dir; delete it manually; re-run — recreates? — verified safe in 0270231: ensureScratchDir uses `mkdirSync(recursive: true)`, is idempotent, and recreates the dir at the same deterministic path after a user `rm -rf`. Regression test walks the create → delete → recreate → no-op → write cycle end-to-end.
- [ ] **Grouping `short_name` gets lost on row recreate** — reported live 2026-07-15: cmux tabs started showing full epic labels (`[Front End] Team-Owned Tokens UI`) instead of the previously-set shortnames (`Team Tokens`). Root cause: pr-watch's `catalogue_sync.py:285` upserts groupings with only `--label` + `--url`. The DB layer's partial-update preserves `short_name` on updates (verified — undefined → preserve), so an existing row keeps its shortname. BUT when a row is created fresh (or was deleted/recreated between ticks), the INSERT path stores `short_name = NULL`. Once NULL, no downstream writer restores it. Live evidence: `.ccs/clusters/pr-watch/cluster/groupings.json.migrated` has all three shortNames populated (dated Jul 14T22:20:50Z), but DB rows were re-inserted Jul 15T18:41:42Z with `short_name = NULL`. Immediate fix: `ccs grouping set <id> --short_name=…` for each affected epic (I did this for the three pr-watch epics live: `Team Tokens`, `PP→Dashboard`, `Metered Pricing`). Real fix options: (a) catalogue_sync derives shortName from label via heuristic + passes `--short_name`; (b) ccs stores a persistent per-cluster shortname-override table the sensor can't clobber; (c) grouping row deletion becomes an error unless `--force`, so the recreate-with-null path can't happen silently. — `.ccs-config/clusters/pr-watch/engine/scripts/catalogue_sync.py:275-296`, `src/state/groupings-db.ts:128-149`. Regression test candidate: catalogue_sync-simulated upsert (label only) into a row whose short_name is populated should preserve short_name — but a full delete+insert cycle should also preserve short_name if the JSON migration's data is still on disk.

Add findings here as you go. Every idea, even ones you rule out, gets a checkbox — checked means "verified safe" or "fixed", not just "considered".

## Constraints

- **Don't break the pr-watch cluster.** It's live. If a change would require a coordinated ccs+ccs-config update, park the change on the branch, note it in the log, and don't commit to master.
- **Every fix ships with a test.** No "verified by hand".
- **Never `git push --force` or `git reset --hard` on any branch.**
- **Never mock `~/.ccs/cache/*.db` writes in tests.** Use `:memory:` or a `tmpdir` — the live DB stays out of test runs.
- **Never post to Slack** or any external service in Phase 1. This is code-only.

## Handoff to Phase 2

When exit criteria are met, this loop:

1. Writes `docs/overnight/PHASE1_DONE` with a summary of what was fixed.
2. Spawns Phase 2 via `ccs session new --cwd ~/.ccs-config --title overnight-signal-scout --prompt "/loop 45m /overnight-dogfood"`.
3. Marks itself completed (`ccs session complete .`) so the phase-1 loop stops on the next tick.

The phase-1 loop is done. Phase 2 takes over.
